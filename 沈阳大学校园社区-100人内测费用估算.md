# 沈阳大学校园社区：100 人内测费用估算

核价日期：2026-08-28；口径更新：2026-08-28。以下是用量假设与官方公开单价的计算，不是账单、采购报价或容量测试结果，不使用新客免费额度抵扣，也不代表已确定供应商及模型版本。

2026-08-28 口径更新：联网搜索已确认改为独立检索适配层（国产 LLM 搜索 API，约 3 元/千次量级），不再使用模型供应商内置搜索；原"内置联网搜索 turbo"单价仅作参考，实际以所选搜索服务价格为准。服务器已确认使用境外亚太优化线路（免备案），100 元预算占位的适用条件相应变化。

## 1. 已确认的预算与规模

- 首批按 100 名已认证用户估算，不等于 100 人每天活跃或同时在线，也未确定为注册硬上限。
- 每月目标预算 200 元：服务器 100 元、其他服务合计 100 元。
- 每人每日最多 10 次完成的 AI 回答。按 30 天算，全部用满为 30,000 次；这只是成功回答的额度上限，不包含失败、重试等额外调用。

## 2. 计价样本

| 项目 | 本次采用的单价 | 官方来源 |
| --- | --- | --- |
| qwen-flash，北京地域，输入不超过 128K | 每百万输入 Token 0.15 元、输出 Token 1.5 元 | [模型价格](https://help.aliyun.com/zh/model-studio/model-pricing) |
| 联网搜索（独立检索适配层，国产 LLM 搜索 API） | 约 3 元 / 千次量级，搜索结果还会增加模型输入 Token | 原内置搜索计费口径已作废，见 [联网搜索计费](https://help.aliyun.com/zh/model-studio/web-search/)；实际以所选搜索服务定价为准 |
| 文本审核大模型基础版 | 20 元 / 万次 | [文本审核计费](https://help.aliyun.com/zh/document_detail/2866911.html) |
| 图片审核大模型版 | 45 元 / 万次 | [图片审核计费](https://help.aliyun.com/zh/document_detail/2857796.html) |
| 国内验证码短信，月发送量不超过 10 万条 | 0.045 元 / 条 | [短信计费](https://help.aliyun.com/zh/sms/product-overview/billing-of-messages-sent-to-chinese-mainland) |

仅选择 qwen-flash 作为成本计算样本，不表示模型质量已通过校园场景评测；计费地域、接口、模型或搜索策略改变后需重新计算。

## 3. 两个使用场景

统一假设：每次完成的回答平均消耗 6,000 输入 Token（包含历史、校内检索及联网内容）和 800 输出 Token，使用非思考模式；50% 的回答各触发一次 turbo 搜索。不计缓存优惠和重试。永久保存历史不代表每次将全部历史送入模型，实际上下文策略尚待设计。

为留出 AI 安全检查费用，另按每次问答的输入、输出各调用一次文本审核估算；这是测算假设，具体审核接入流程仍需确认。

| 月度项目 | 轻度场景：20 人每日各问 3 次 | 用满场景：100 人每日各问 10 次 |
| --- | ---: | ---: |
| 完成回答数 | 1,800 次 | 30,000 次 |
| 模型生成费 | 3.78 元 | 63.00 元 |
| 搜索费 | 2.70 元 | 45.00 元 |
| 模型与搜索小计 | 6.48 元 | 108.00 元 |
| 问答输入、输出审核 | 7.20 元 | 120.00 元 |
| 社区文本审核：假设两场景均为 6,000 次 | 12.00 元 | 12.00 元 |
| 图片审核：假设两场景均为 1,000 次 | 4.50 元 | 4.50 元 |
| 短信：假设两场景均为 200 条 | 9.00 元 | 9.00 元 |
| 服务器预算占位，非已核定报价 | 100.00 元 | 100.00 元 |
| 已列项目小计，非总账单 | **139.18 元** | **353.50 元** |

为便于比较，两列的社区审核量和短信量保持相同，不是对高活跃场景的完整预测。社区文本次数包括帖子、评论、私信等实际提交到审核服务的请求；图片按调用数而非帖子数计费。编辑重审、举报复核、重试、长文本分段或多审核服务组合可能增加调用数。

模型费用公式：回答数 ×（6,000 × 0.15 + 800 × 1.5）÷ 1,000,000。

搜索费用公式：回答数 × 50% × 3 ÷ 1,000。

## 4. 尚未计入与风险

- 对象存储容量、图片下载流量、请求、备份与日志归档、向量化等尚未计价。OSS 不仅收存储费，还可能产生请求与外网流出费用，不能仅按图片占用空间估算。[OSS 计费组成](https://help.aliyun.com/zh/oss/billing-overview/)
- 服务器的实际规格、带宽和购买条件未确定，100 元只是预算占位；已确认使用境外亚太优化线路（免 ICP 备案），自托管数据库与 Redis 是否能承载目标负载仍需压测，境内访问延迟与稳定性风险已知悉。
- 域名等一次性费用未计入，人工开发、认证审核和运营时间也未折算成现金费用。
- 用户失败不扣额度，不表示外部服务一定不收费；需要在内部用量账中核对失败、超时、重试等请求，不能只统计成功回答数。
- 对话保留、图片和安全日志归档会持续占用空间，首月估算不能直接作为长期固定成本。

**判断：轻度场景有剩余预算覆盖尚未计价项目的可能，但不能据此承诺 200 元足够。用满场景仅模型与搜索已达到 108 元，超过其他服务合计 100 元的预算，更未包含审核等费用。**

## 5. 暂缓的成本控制议题

用户要求先不继续讨论预算限额，转为确认后台配置不同 AI API 的能力。此前“接近预算时通知并暂停 AI 问答”的建议未获确认，不作为已确定的首版规则；每日 10 次及回答完成扣次的既有规则保持。

本文件只保留为场景估算参考。后续切换供应商、模型或搜索服务时重新核价，不将 qwen-flash 的样本单价视为所有接口的固定成本。

不应通过跳过内容审核来节约费用；若审核服务不可用，应按既定规则保持相关内容不公开或私信不送达。账单可能延迟，预算控制还需要请求预估、并发预占与安全余量，不能保证到达某个账单数字后才停用就绝不超支。
