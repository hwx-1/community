# 沈阳大学校园社区：开发基线与技术栈

整理日期：2026-08-28。技术方案拷问确认更新：2026-08-28（含当晚第二轮拷问）。前端设计系统改造落地更新：2026-08-29。

状态：需求与规则已按拷问锁定；社区 Web 壳已完成设计系统级重构（React 18 + Vite + TypeScript + CSS Modules，小红书品牌红外壳 + 知乎式信息流 + 日夜间双主题，见第 9 节 2026-08-29 修订与 9.1），桌面三栏与手机 Web 单栏均已落地。尚未接入真实短信/AI 服务或部署上线。用户已明确要求 Go 后端；社区 Web 首版为纯 React DOM。Android、鸿蒙、iOS 原生仍暂缓。本文区分已确认规则与建议默认值；拷问确认的决策以"已确认"标注。

## 1. 项目边界

- 用户：沈阳大学校内学生；首版仅覆盖这一所学校。
- 首批按 100 名已认证用户进行内测规模与成本估算；这不是 100 人同时在线或 100 人每日活跃的已验证数据，也尚未确定为注册硬上限。
- 目标：一个月内完成 Web 首版。已确认端范围：电脑浏览器以三栏为设计基准；手机浏览器使用同一套 React DOM 页面做知乎式单栏，并采用五入口底栏；这仍是响应式 Web，不是原生 App 原型。Android、鸿蒙、iOS 原生分阶段交付，首版不做。
- 初始月度运行预算目标为 200 元：服务器 100 元，短信、AI 问答、内容审核、图片存储等其他服务合计 100 元。已完成假设场景测算，尚无真实流量账单或服务器压测验证；详见同目录《沈阳大学校园社区-100人内测费用估算.md》。
- 后续根据实际使用和承载情况考虑增加预算或扩容，具体调整另行确认，不自动开通或购买额外付费资源；域名等一次性费用的预算归属待确认。
- 已确认 Web 信息架构：首页、百宝箱、消息、我的；发帖为独立页（2026-08-29 修订：电脑端仅保留左栏一个入口，顶栏发帖入口已移除并改为日夜间切换按钮）。电脑顶栏搜索框居中显示、内嵌搜索按钮（2026-08-29 确认）。2026-08-29 续补确认：手机 Web 采用知乎式单列，底部固定五栏「首页 / 工具 / 发布＋ / 消息 / 个人页」，中间发布键进入独立发帖页；顶栏保留品牌、主题切换和搜索图标。右栏不在手机常驻，百宝箱进独立页，公告和热门话题仅在首页做成可折叠块。
- 移动端原生信息架构（后续）：首页、聚合工具页、发布页、消息页、个人页，五页 + 凸起发布键。移动端开发暂缓；原型已完成，后续启动时按 React Native 原生重建、复用后端接口，不与首版 Web 共享 UI。
- 已确认部署方向：云服务（短信、对象存储、内容审核、AI）全栈阿里云；应用服务器使用境外亚太优化线路（免 ICP 备案），域名使用已有 `xsnbb.xyz`，HTTPS 采用 Let's Encrypt + Nginx 终结。境内访问延迟与稳定性风险已知悉。
- 已确认短信签名：个人资质 + 中性品牌签名 `xsnbb`，不出现"沈阳大学"字样，与"不冒称官方"规则一致。
- 首版不做付费置顶。保留管理员手动置顶为当前开发默认项。
- 首版不引入多校隔离、支付、订单、群聊、音视频通话、视频发帖或模型训练。
- 不将自建社区宣称为学校官方平台；校方通知、平台公告及认证主体要准确区分。

## 2. 已确认的业务规则

### 2.1 注册、准入与资料

- 注册使用手机号、短信验证码、密码和有效邀请码。
- 邀请码为固定共用邀请码，超级管理员可在后台修改；更换后旧码不能用于新注册，既有账号不受影响作为实现默认值。
- 同一手机号两次短信发送至少间隔 120 秒；验证码签发后有效期 5 分钟；单个验证码累计输错 3 次失效。
- 验证码失效不等于冻结用户账号；重新获取继续遵守发送间隔。服务端执行时间和次数检查。
- 已确认忘记密码：已绑定手机号的账号用「手机号 + 短信」重置密码；同一账号只有一个密码，重置后社区与后台共用该密码。未绑手机的管理员不能自助重置，由超管在后台代设。超管本人未绑手机时，首版只允许运维改库，不提供在线找回，也不允许用启动环境变量反复改密。
- 注册后资料未完善，或沈阳大学学生身份认证尚未通过时，开放浏览、举报、资料修改及认证申请，暂不开放发帖、评论、点赞、收藏、私信和 AI 问答。
- 学生认证待审核或被驳回时仍按未通过处理；资料完善且认证通过后才满足上述功能的基础准入条件，同时继续遵守禁言、封禁及各功能自身限制。
- 必填资料：姓名、头像、性别、学号、班级；另外设有公开昵称。已确认昵称约束：全站唯一，2–16 字符，允许中英文、数字、下划线，禁止纯空白；数据库加唯一索引，注册/改名时处理重名冲突。
- 已确认性别枚举：仅「男」「女」，公开主页照实显示；不做「保密」或「其他」。
- 已确认可自行修改（不重新认证）：头像、性别、昵称、班级。认证通过后仍锁定真实姓名和学号。
- 对其他用户展示昵称、头像和性别；真实姓名、学号、班级为内部资料。
- 已确认公开主页：他人可点头像/昵称进入；只显示头像、昵称、性别、认证徽标，以及该用户已公开的帖子。不显示收藏、待审、学号、班级、手机号、真实姓名。
- 已确认「我的」页：资料卡（头像、昵称、性别、认证状态）+ 我的帖子 + 我的收藏 + 编辑资料 + 学生认证/变更申请 + 注销账号 + 退出登录。有后台权限的账号额外显示「进入管理后台」（跳转独立后台，见 2.10）。
- 内部资料允许本人、超级管理员以及明确获得相关权限的人员查看。
- 首版必须进行沈阳大学学生身份认证；用户上传相关证明材料（学生证或学信网信息截图，二选一），认证通过后才能发帖。
- 已确认证明材料张数：每次申请 1 张，拍有姓名、学号、学校信息的那一面即可，不要求正反面。驳回再交仍是 1 张。格式与大小见 2.3 图片规则。
- 所有认证申请均由管理员人工审核，审核权限由超级管理员分配；不由 AI 自动批准认证。
- 建议证明材料仅用于身份核验，私有存储且不进入公开个人页、帖子、搜索或 AI 问答知识库。已确认材料访问边界：仅被超级管理员授予"学生认证审核"权限的管理员可查看证明材料，且仅在审核该申请时可见；查看行为记入操作日志；超级管理员默认可见。
- 证明材料从该次认证审核结束起保留 30 天，之后自动删除；删除证明材料不影响已通过的认证状态。认证结果与证明图片分开管理。已确认认证记录（谁、何时、通过/驳回、操作者、学号绑定）长期保留；备份中的证明图随备份轮转到期一并消失，不做「图永存、只藏起来」。
- 建议允许遮挡身份证号等与本次认证无关的信息，不收集学信网账号、密码或登录凭证。已确认核验标准：管理员人工目视核对，检查材料中的姓名、学号与用户填写的是否一致，材料是否为沈阳大学有效证件（学生证/学信网截图）；不要求联网验证学信网。
- 已确认驳回补交规则：认证被驳回后，用户可修改证明材料重新提交，重新进入人工审核队列，不限次数；认证状态机支持"驳回→待提交→待审核"循环。
- 已确认待审材料超时处理：待审核材料不设自动过期，一直保留直到管理员处理；后台对待审超过 7 天的申请做高亮提醒。
- 同一学号同一时刻只允许绑定一个已认证账号；后台支持绑定和解绑学号，操作权限由超级管理员控制。
- 建议在数据库层约束学号的有效绑定唯一性，绑定变更通过事务执行并记录操作者、时间和原因。
- 解绑学号后立即撤销该账号的学生认证；重新绑定学号本身不恢复资格，须重新通过管理员人工认证，才可恢复符合其他准入条件的互动和 AI 问答。已有禁言或封禁仍然有效。
- 认证通过后锁定真实姓名和学号，用户不能自行修改，需向管理员申请变更。已确认变更核验流程：用户提交变更申请（说明原因 + 新证明材料 1 张），由认证审核角色管理员核验新材料后手动修改；变更记入操作日志，复用认证审核角色。班级不锁定，本人可随时修改。
- 已认证学生毕业后保留认证资格，可继续使用符合其他准入条件的发帖、互动和 AI 问答功能，不因毕业自动撤销认证。
- 已确认首版提供账号注销功能：用户可申请注销，注销后账号标记停用，内部资料（姓名/学号/班级）物理删除；已发帖子/评论匿名化（作者显示"已注销用户"）但内容保留；私信对对方仍可见但发送方显示已注销。已确认学号释放：注销后该学号可被新账号重新注册并重新认证；主库认证记录匿名化；「此学号曾经注销过」仅保留在安全审计日志中。
- 已确认学生换号：首版不做自助换绑。学生通过站外联系超管，由超管在后台改绑到未占用的新号；旧号释放；学号不变则学生认证保留。丢号无法登录时也走这条人工路径。

### 2.2 认证徽标与权限

- 用户提交认证申请，由超级管理员或其授权的管理员人工审核；对应徽标由超级管理员配置。
- 徽标与操作权限独立；认证通过不自动增加权限。
- 额外功能权限由超级管理员独立授予或撤销。
- 运营团队设一名超级管理员和多名管理员；所有管理权限由超级管理员下发，管理员可承担不同方向的职责。
- 普通管理员仅能使用明确获授的权限，不能自行扩大权限或向其他账号转授权。
- 支持超级管理员创建自定义职责角色、为角色配置权限，并给管理员分配一个或多个角色；角色及授权均由超级管理员管理。
- 学生认证审核、内容审核、举报与申诉处理、校园资料维护等可作为职责划分示例，不默认让任一管理员获得全部职责或敏感资料访问权限。
- 建议将学生身份认证与认证徽标分开建模：学生身份认证用于发帖准入，认证徽标不自动授予后台权限。
- 建议使用权限点和授权记录。认证状态、身份资料读取权限、内容管理权限、私信正文访问权限分别判断。

### 2.3 帖子、标签与互动

- 支持文字、最多 9 张图片和标签。已确认必填条件：帖子文字必填（1–2000 字），图片可选（0–9 张）；标签可选。
- 已确认发帖入口：独立发帖页，不在信息流顶部内嵌编辑器。2026-08-29 修订：电脑端仅保留左栏一个「发帖」入口，顶栏发帖入口已移除（顶栏右侧改为日夜间切换按钮）；手机 Web 使用底栏中间凸起的「发布＋」入口。编辑已发布帖子走同一页。
- 已确认图片格式与大小（帖子、评论、头像、认证材料同一套）：允许 jpg/png/webp/heic；HEIC 在服务端转成 jpeg 再存储、再送审；单张 ≤ 10MB；不支持 gif 动图和视频。
- 已确认文字长度上限：帖子正文 1–2000 字，评论/回复 1–500 字；数据库用 text 存储 + 服务端校验，前后端分别限制。
- 支持点赞、评论、收藏、搜索。
- 收藏列表仅本人可见，不向其他用户展示，也不默认向管理员开放收藏清单。
- 评论区采用主评论和回复两层结构，所有回复集中在所属主评论下面，不继续增加展示层级。
- 评论与回复支持文字和图片，每条最多 1 张图片。已确认纯图规则：评论/回复文字必填（1–500 字），图片可选（0–1 张），不允许纯图片，与帖子规则一致。
- 评论和回复发布后不可编辑；作者可以删除后重新发布，新内容重新审核。
- 首页普通帖子仅按发布时间倒序，不提供点赞排序或排序切换；编辑不刷新首次发布时间作为实现默认值。
- 管理员置顶独立展示，不参与普通帖子排序；点赞互动功能仍然保留。
- 用户选用已有标签，或在发布新帖时创建不存在的标签；超级管理员可创建、删除标签。已确认标签上限：每帖最多 3 个标签，标签名 1–10 字。
- 标签在帖子成功提交业务事务时建立，内容在审核通过前不应出现在公开标签列表中。
- 删除标签默认仅删除标签及关联关系，不删除帖子。已确认同名重建：未删除的标签名全站唯一；软删后名字释放；旧帖上的已删标签不再可点，不会自动挂到新创建的同名标签上。
- 转发定义为复制帖子固定链接，不生成站内转发帖。
- 已确认搜索范围：只搜已公开帖子的正文和标签名，结果为帖子列表。不搜评论、用户昵称、私信、AI 会话。未公开、已删除内容不得出现在结果中。
- 未登录打开帖子链接，显示“请先登录”；登录后返回原帖。未完善资料的已登录用户仍可浏览。
- 作者允许编辑已发布帖子。编辑稿重新审核，公开旧版本继续展示，新版本审核通过后替换。
- 帖子实体与内容版本分开存储，点赞、评论、收藏继续关联同一个帖子。
- 用户可以删除自己发布的帖子、评论和回复；作者删除与管理员下架分别记录。
- 已确认「我的帖子」：作者能看到自己的待审核、未通过、已公开、已下架/已删除。未通过显示原因，可从同一发帖页修改再提交。待审/未通过只有作者和有审核权限的管理员能看。
- 删除单条主评论后，原文字与附图不再展示，保留“该评论已删除”的占位；其他用户已有的回复继续可见。已确认：主评论被删除后禁止在该主评下新增回复，已有回复仍可见。
- 删除整个帖子时，帖子及其全部评论、回复一并删除，不保留讨论区；首版不支持恢复已删除的帖子。
- 删除内容不能继续通过分享链接、搜索、收藏或旧通知展示；关联入口可显示“内容已删除”。已确认清理规则：删除先软删（标记删除，立即对外不可见），30 天后定时任务物理清理数据库记录和 OSS 图片；软删期支持管理员审计和误删排查，不以无限期隐藏代替数据清理。所有内容表加软删除字段，并区分作者删除与管理员下架类型。

### 2.4 帖子与评论审核

流程：提交 → 内置违禁词检查 → AI 内容检查 → 自动检查通过后公开并等待人工复核。

- 检查范围包括正文、标签、图片内容及图片中的文字。
- 命中风险时暂不发布，向作者提示风险类别和原因；提供修改及人工复核入口作为实现建议。
- AI 超时、失败或无法明确判断：暂不公开，转人工处理；明确人工通过后才可公开。
- 自动检查通过后的内容可先公开，后台状态保持“已公开、待人工复核”，不能伪记为人工已通过。
- 管理员发现问题可以立即下架，并保留原因、操作者和记录。
- 评论沿用相同规则；未公开评论不应提前出现在他人的评论列表、通知正文或搜索结果中。已确认：作者在原帖评论区能看到自己的待审/未通过评论（带状态和原因），其他人看不到；通过后变成正常楼层。首版不做「我的评论」独立页。
- 原帖下架是独立控制条件：编辑任务晚到的审核回调不能让已经下架的帖子自动恢复。
- AI 输出属于风险判断，不是违法认定。具体内容治理规则和适用法律需单独核验，供应商审核结果不构成平台合规保证。

### 2.5 私信

- 首版为一对一文字私信。
- 第一次联系仅允许发起方发送一条系统内置消息；接收方未回复前不能追加或重复发起。
- 接收方也回复一条内置消息后，双方才能发送自由文字。
- 私信正常发送时经过违禁词与 AI 检查后送达，不进入人工审核队列；收到举报后按举报流程处理，自动无法判定时可以转人工。
- 已确认私信 AI 异常重试规则：AI 审核异常/超时时，私信保持"未送达"状态，前端显示"发送失败，点击重试"，由用户手动重试；不自动重发，避免重复扣审核费和重复送达。
- 已确认内置招呼文案：首次联系的内置消息文案由超级管理员在后台配置，可修改；需要配置表和后台界面。
- 超级管理员能够在后台查看双方完整私信正文。此权限与身份资料查看授权分开，不自动授权其他人员。
- 建议记录后台访问日志，并明确告知用户存在管理访问；不能宣称服务端无法读取或端到端加密。
- 拉黑、举报、未读状态、历史消息和发送失败重试为首版配套建议。已确认拉黑规则：单向拉黑；A 拉黑 B 后，B 不能向 A 发新私信（服务端拒绝，提示发送失败但不透露被拉黑），已有历史消息双方仍可查看；A 可随时解除拉黑，不通知 B。

### 2.6 百宝箱与 AI 问答

- 首页展示百宝箱入口和工具排行区域，百宝箱页承接完整列表。
- 已确认对外名称：「百宝箱」。导航、右侧栏、工具页统一使用该名称，不再使用「校园工具」。
- 工具展示顺序由超级管理员手动配置，不自动按使用次数、使用人数或热度计算。
- 工具包括校园地图、AI 问答和常用网址。已确认工具配置字段：名称、图标、跳转链接/类型、排序权重、启用状态；超级管理员后台支持工具的添加、编辑、删除和排序。
- 已确认热门话题：超管在后台手动选择已有标签及展示顺序，不按发帖量或点赞自动计算。电脑右栏与手机首页折叠块使用同一份配置。
- 已确认平台公告：独立模块，由超管后台发布，右栏（及手机首页折叠块）展示最近若干条；不能评论、点赞。与管理员置顶帖不是同一对象，也不自动等于知识库中的校方通知。校方通知只进入知识库供 AI 使用。
- 校园地图首版以一张图片展示；放大查看和后台替换为建议配套能力，不接入定位或路线规划。
- AI 首批回答部门联系电话和官方发布内容。
- 首版用户端 AI 问答仅支持文字输入，不支持上传图片或文件。
- 后台支持配置不同 AI 服务的 API，不绑定单一问答供应商；配置权限仍由超级管理员授予。
- 建议配置项包括服务名称、接口类型、Base URL、API Key、模型标识和启用状态；凭证加密保存在服务端，界面脱敏展示，不下发到用户端或写入日志。
- 不同接口协议通过 Go 后端适配；新增配置不代表任意供应商已兼容，也不自动替换独立的内容审核服务。已确认适配层首版仅实现 OpenAI 兼容协议（DashScope 的 qwen 系列支持 OpenAI-compatible 模式），配置字段即服务名称、接口类型、Base URL、API Key、模型标识和启用状态；供应商与默认模型均为后台运行时配置，不在开发期锁死。
- 普通用户可以从后台已开放的模型列表中自行选择 AI 模型；服务端应校验所选模型仍处于可用且对用户开放的状态。
- 允许在同一会话中途切换模型，由新模型读取该会话的必要上下文并继续回答。
- 已确认上下文策略：按目标模型的上下文上限，从最近轮次往前塞入；超出的旧轮次本次不发送，库中仍保存。换模型使用同一套截断，不做摘要压缩。界面不假装模型读过被截掉的旧消息。
- 选中的模型服务不可用时自动切换备用模型；备用模型及切换优先顺序由后台统一配置，配置权限遵循超级管理员授权。
- 建议自动切换用于超时、限流、服务异常等可恢复故障，不将内容安全拦截作为换模型绕过检查的理由，也不能因切换而跳过既定审核流程。
- 建议向用户显示切换提示并标明实际回答模型；所有可尝试模型均失败时提示服务异常，按既定规则不扣该次用户额度。已确认故障切换策略：单次回答最多尝试 2 个模型（首选 + 1 备用），每个模型超时 30 秒；若首选模型已开始流式输出再失败，不切换、不重试，直接提示"回答中断，本次不扣额度"，避免半截回答拼接混乱。
- 建议为每条回答记录并展示实际使用的模型，切换不覆盖旧回答；模型上下文适配或截断不应删除用户已保存的会话历史。
- 用户要求暂不继续讨论预算限额，未确认预算触发停用 AI 的方案；此前每日 10 次、回答完成扣次的规则保持，不新增按预算自动停用规则。
- 供 AI 使用的部门电话和官方资料由后台配置、录入和更新。
- 每位用户每天有 10 次免费 AI 问答额度，每日 00:00 刷新；时区按北京时间（Asia/Shanghai）执行。已确认跨日额度归属：按请求发起时刻判定；用户 23:59 发起、00:01 完成的问答，扣发起当天的额度。
- AI 回答完成后才扣除 1 次额度，失败或超时不扣；请求开始预占、完成后确认扣减、失败时释放预占为建议实现机制。
- AI 支持同一会话内连续追问，每次完成的回答分别扣除 1 次额度，不以整个会话作为一次计费单位。
- AI 对话历史按账号保存，用户在不同设备登录同一账号后，可继续原会话；首版在不同 Web 设备间支持，后续移动端接入同一账号会话服务。
- 建议历史读取和跨设备同步不扣问答额度；服务端按账号统一计算每日额度，并保证请求重试不重复扣次。
- 完整 AI 对话历史仅对用户本人开放；后台只展示进入待补充列表的问题，不提供整段会话查询，包括超级管理员在内的管理账号也不因此获得完整会话查看权限。
- 用户可删除整段 AI 会话，删除后各设备同步移除，不提供恢复入口；服务端停止提供该会话的历史读取和续聊，离线设备重新联网同步时移除对应会话。
- 已确认删除会话后的残留：主库立刻停止读取和续聊；备份随日常轮转过期，首版不从全部备份立刻擦除；已经发到消息页的官方回答通知保留，点开展示问题摘要 + 官方补充答案，不进入已删会话；知识库中管理员撰写的答案保留。待补充问题仍按既定规则撤回，不再发后续通知。
- 用户未主动删除的 AI 对话历史持续保留，不设置自动过期或定期清理期限；这不代表已删除会话的副本也永久保留。
- 删除会话时，一并撤回该用户在该会话中关联的尚未处理的待补充问题，停止向该用户发送这些问题的后续官方回答通知。
- 建议撤回状态与通知发送资格一致更新，发送前再次校验，避免延迟任务在删除后继续发送；按用户与会话关系撤回，不影响其他用户独立提交的问题。
- 删除后的存储与备份清理已确认见上；账号注销时的 AI 会话按注销规则停止提供，已发送的官方回答通知和已纳入知识库的内容不因注销或删会话而收回。
- 首版每日额度用完后暂停新的 AI 问答，等待刷新；充值活动列入后续版本，首版不接支付。
- 电话等精确字段从维护的结构化目录读取；通知检索后回答，并展示来源、日期。已确认知识库检索方式：结构化 + 关键词检索（pg_trgm），不安装 pgvector、不做向量 RAG、无 embedding 费用；知识库规模小（部门电话 + 官方通知），未命中再走联网搜索。
- 校内知识库没有答案时，允许联网搜索并回答，同时在后台记录待补充的问题。已确认联网搜索路线：走独立检索适配层（后端自调搜索 API，结果拼进 prompt 再给模型），不使用模型供应商内置搜索；已确认选用国产 LLM 搜索 API（约 3 元/千次量级），不绑定模型供应商。费用估算文档中"内置搜索"口径相应作废，以独立搜索服务实际价格为准。
- 建议将后台状态区分为“校内资料未覆盖”和“联网后仍未能回答”，记录联网来源；涉及校内事务优先参考学校官方资料，没有可靠来源时明确说明。
- 管理员补充答案后，通知未撤回相应问题的原提问用户，在消息页作为“官方回答”通知展示；因删除会话已撤回的问题不再向该用户发送后续通知。已确认待补充问题合并规则：后台对相似问题做文本去重/合并，同一问题只留一条并记录提问人数；管理员补充答案后通知所有未撤回的提问者。
- 建议通知关联原问题、补充答案、实际答复主体及更新时间；保留原 AI 回答记录，不覆盖历史答复。
- 联网搜索属于同一次问答，回答完成只扣 1 次额度；搜索请求不附带后台真实姓名、学号等身份资料。
- 知识库仅收录授权维护的资料；普通学生发帖和私信不自动成为官方事实来源。已确认种子数据来源：部门电话、官方通知等资料由项目方从沈阳大学官网等公开渠道收集，内测前录入后台；开发侧提供批量导入入口和录入界面。
- AI 接入和配置可安排在开发后段，但内容审核服务必须在相关发布功能对真实用户开放前准备好。

### 2.7 消息页

- 已确认的消息类型：私信、官方回答、评论通知、举报处理结果通知。
- 评论及其图片通过审核后才展示并发送通知；未公开内容不提前推送正文。
- 主评论通知帖子作者作为默认规则。已确认回复通知规则：回复（楼中楼）只通知被回复的那个人（回复主评论→通知主评作者；回复某条回复→通知那条回复的作者），不重复通知帖子作者。已确认通知合并与已读规则：同一帖子下多人/多次评论回复在消息页合并为一条聚合通知（如"3 人评论了你的帖子"），点击展开列表；点开聚合通知卡片即整条标记已读，未读数按卡片计。
- 官方回答通知关联原提问和后台补充答案，查看通知不消耗 AI 问答额度。已确认：通知自身带问题摘要 + 官方补充答案；原 AI 会话已删除时点开通知仍只看这一条，不进入会话。

### 2.8 举报与临时隐藏

- 首版支持举报帖子、评论和私信；用户明确要求任何用户均可举报。
- 实现默认将“任何用户”解释为所有已登录用户，包括资料未完善的账号；举报对象必须是该用户有权访问的内容，不能借举报读取他人私信。
- 首次收到针对某一内容版本的举报时，立即暂时隐藏被举报内容，依次执行内置违禁词检查与 AI 判断；自动无法明确判断时转人工，处理期间保持不展示。
- 复核确认没有违规后自动恢复展示。
- 同一内容版本已复核确认无违规后，再次收到举报先保持展示，交由管理员判断是否需要重新隐藏；这是“收到举报立即隐藏”的例外，仅适用于已复核通过的同一版本。
- 恢复只解除本次举报产生的临时隐藏，不撤销作者删除、独立管理员下架或其他尚未结束的隐藏限制。
- 私信举报的人工处理不能自动授予普通审核人员读取私信全文的权限；具体分配遵守独立授权，未另行授权时仍由超级管理员处理。
- 建议配置举报频率限制、同一用户同一对象去重和操作记录，避免单次举报隐藏机制被滥用；不单凭举报数量认定违规。
- 被举报用户看不到举报人身份；超级管理员可以查看举报人信息。其他审核人员默认不获得该查看权限，通知与普通接口不得泄露举报人信息。
- 举报处理完成后，通过消息页分别通知举报人和被举报内容的作者。
- 建议结果通知说明处理结论与内容状态，不包含举报人身份或已被限制展示的正文、图片；对私信也遵守原有访问权限。
- 警告、禁言、封号由超级管理员或获授权人员手动执行，不根据 AI 判断或举报次数自动处罚账号。
- 用户可以从内容下架或账号处罚通知中提交申诉，由管理员人工复核。已确认申诉规则：每次处罚只能申诉 1 次，管理员需在 7 天内处理；超时未处理则申诉保持待审状态，不自动通过。
- 封禁账号仍可登录，但仅能查看处罚通知、提交申诉和查看申诉结果，不能访问其他社区功能。已确认封号不影响旧内容：已公开的帖子/评论默认仍公开，作者仍显示原昵称，公开主页仍可看其已公开帖。要隐藏内容须另走下架或删除。解封也不自动改内容状态。
- 禁言禁止发帖、编辑帖子、评论（含回复）和发送私信；浏览、点赞、收藏、AI 问答、举报和申诉不因禁言而关闭，但仍须满足各功能原有的准入条件。
- 处罚时长、申诉次数与处理时限已确认：禁言提供固定档位（1 天 / 3 天 / 7 天），封号仅永久一档；管理员选档位而非自定义时长。处罚表存档位而非任意时长，禁言到期需自动解除任务。

### 2.9 后台操作日志

- 管理员的管理操作设专门的后台操作日志，超级管理员自身的管理操作也纳入记录。
- 记录范围包含角色配置、授权变更、认证审核、内容下架、账号处罚，以及查看认证材料或私信等敏感管理操作。
- 建议提供独立的操作日志页面，支持按操作者、时间、操作类型、目标对象和结果筛选；日志查看权限单独控制。
- 建议记录操作者、操作时间、目标标识、操作结果、必要原因和请求标识；不将证明图片、私信全文、密码或验证码写入操作日志。
- 经确认，后台操作日志默认展示最近 15 天；必要的安全审计日志另行归档至少六个月。“默认展示 15 天”不代表将全部日志在第 15 天物理删除。
- 上线约束：现行《网络安全法》第二十三条第三项要求相关网络日志留存不少于六个月。操作日志中涉及安全的记录需核验分类及适用期限，不能仅因命名为“操作日志”就适用 15 天删除。[国家网信办公布的法律全文](https://www.cac.gov.cn/2025-12/29/c_1768735112911946.htm)
- 其他有保留要求的记录按适用期限处理。
- 已确认日志防篡改机制：操作日志表仅追加（无 UPDATE/DELETE 接口），后台不提供修改/删除入口，应用层不暴露写接口；100 人内测阶段满足"仅追加"原则。
- 已确认日志两档：`security`（登录/鉴权异常、授权变更、查看认证材料、查看私信、下架、处罚、注销相关、学号曾注销记录）归档至少 6 个月；`operational`（其余后台操作）默认展示 15 天，90 天后物理删除。首版不做第三套分类 UI。
- 已确认归档查询权限：做成权限点「查看安全审计归档」，默认只有超管拥有；超管可授给某个管理员，不预置「审计员」角色。

### 2.10 管理员账号、登录与会话

- 已确认运营账号与学校账号分开创建：超级管理员由启动配置种子创建；其他管理员由超管在后台创建。创建时不走邀请码、不强制学生认证，默认只能进入管理后台。
- 已确认第一个超管：环境变量提供登录名 + 密码；API 首次启动发现库中没有超管时创建一次；之后不能靠同一配置反复重置密码。
- 已确认管理员登录：后台始终使用「登录名 + 密码」。创建时设全站唯一登录名，不强制手机号。
- 已确认同一账号可补社区身份：管理员可在后台绑定手机号（短信验证）后，再走资料完善与学生认证；认证通过后才能在社区发帖。未绑手机不能进入社区。管理员走学生认证时，证明材料规则与普通学生相同（每次 1 张、有信息的一面）。
- 已确认手机号全站唯一（学生注册与管理员绑定同一约束）。注册或绑定发现已占用则失败，提示「该手机号已被使用」，不说明占用方身份。首版不做账号合并，也不把学生号升级成管理员。
- 已确认解绑手机（本人或超管）：号码释放、不能再登录社区、学生认证立刻撤销；后台登录名 + 密码不受影响。已发帖子仍挂在该账号上。再进社区须重新绑手机并重新人工认证。
- 已确认两套会话：社区登录不能进入后台；「我的」中的「进入管理后台」跳到独立后台，须用登录名 + 密码再登。后台登出不影响社区，反之亦然。首版不做单点登录。同一账号一个密码。

## 3. 技术栈

主方向遵循用户选择：Go 后端、独立端能力适配。移动端暂缓后，社区 Web 首版已确认改为纯 React DOM（原 React Native Web 方案调整，移动端后续用 RN 原生重建、复用后端接口）。其余组件为建议选型。Web 首发，三端后续交付；不将尚未验证的移动端适配当作已经实现。

| 层级 | 建议选型 | 用途与边界 |
| --- | --- | --- |
| 社区 Web | React、TypeScript、Vite（纯 React DOM） | 已确认弃用 React Native Web。电脑三栏；手机浏览器为知乎式单栏 + 底部五栏 + 顶栏搜索。Web 壳已完成设计系统级重构（2026-08-29，见第 9 节）。后续移动端启动时用 RN 建原生 App，复用后端接口 |
| Android、iOS | React Native、TypeScript、Metro | 后续建设原生宿主工程，复用接口与业务逻辑；不与首版 Web 共享 UI 组件 |
| 鸿蒙 | React Native OpenHarmony 适配路线，必要时补充 ArkTS 原生模块 | 单独验证目标 HarmonyOS 设备、SDK 和 RN 版本的匹配，不保证全部插件自动兼容。[官方平台说明](https://reactnative.dev/docs/out-of-tree-platforms) |
| 路由与状态 | Web 使用 React Router；Zustand、TanStack Query | 状态与查询逻辑 |
| 社区组件与样式 | CSS Modules、共享设计变量 | 品牌红（#ff2442）+ 知乎式信息流 + 日夜间双主题视觉规范（2026-08-29 修订，替代原蓝色系），见第 9 节 |
| 管理后台 | React DOM、Ant Design、独立 Web 管理入口 | 后台不要求原生跨端；复用 API 与权限规则，不以隐藏菜单代替服务端鉴权 |
| 后端 | Go、Gin | 模块化单体 API，耗时任务由独立 Worker 处理。[Gin 官方介绍](https://gin-gonic.com/en/docs/introduction/) |
| API 协议 | REST、OpenAPI | 基于协议生成客户端类型；Go 与 TypeScript 通过契约协作，不直接共享服务端实现 |
| 关系数据库 | PostgreSQL、GORM、版本化 SQL 迁移（goose） | 保存账号、帖子版本、审核、标签、私信与权限；生产迁移可追溯，不仅靠启动时自动变更。已确认迁移工具为 pressly/goose |
| 限流与异步任务 | Redis、go-redis、Asynq | 短信间隔、验证码尝试次数、会话、审核队列与重试。[Asynq 是 Redis 支持的 Go 任务队列](https://github.com/hibiken/asynq)，按至少一次执行设计幂等 |
| 消息更新 | 标准 WebSocket、REST | REST 提交待审核消息，通过后推送；断线通过历史游标补拉，不能只依赖实时连接 |
| 图片存储 | 私有对象存储，阿里云 OSS | 上传凭证、审核隔离、授权读取；HEIC 服务端转 jpeg；避免公开图片地址绕过登录与下架规则 |
| 普通搜索 | PostgreSQL 查询，按需增加 pg_trgm | 仅已公开帖子正文与标签名；中文短词匹配单独测试，不宣称默认具备完善中文分词 |
| AI 问答 | 后台可配置的 AI API、Go 供应商适配层（首版 OpenAI 兼容协议）、结构化 + 关键词检索（pg_trgm）、独立联网检索适配层（国产 LLM 搜索 API） | 通义千问仅为候选及费用测算样本，不锁定供应商。已确认不安装 pgvector、不做向量 RAG。优先查询校内资料，未命中时联网并记录待补充问题。具体供应商和协议需逐项验证 |
| 内容审核 | 独立审核适配层，优先评估阿里云内容安全增强版大模型审核 | 与校园问答分开，保留失败和不确定状态。[供应商提供文本、图片审核大模型接口](https://help.aliyun.com/zh/document_detail/2878282.html) |
| 短信 | 阿里云短信服务，Go SDK 或服务端 API | 已确认个人资质 + 中性品牌签名 `xsnbb`；真实接入需要账号、签名、模板及审批；测试模拟不算真实短信接入完成 |
| 登录与权限 | Argon2id、两套 HttpOnly/Secure Cookie 会话（社区 / 后台分离）、CSRF 防护、Go 权限中间件 | 社区与后台分别建会话，互不自动登录。移动端认证与安全存储后续独立适配；认证徽标不自动授予权限 |
| 测试 | Web 的 Vitest/Playwright，Go 的 go test/httptest，移动端后续增加 RN 测试 | 覆盖鉴权、短信边界、审核转换、私信握手与页面流程 |
| 工程与部署 | pnpm workspace、Go Modules、Docker Compose、Nginx、CI | 前端锁文件和 go.mod/go.sum 分别管理；配置与密钥不进入前端产物 |
| 运行观察 | 结构化日志、健康检查、错误跟踪、备份与恢复演练 | 普通日志不记录验证码、密码、密钥、会话凭证或私信全文 |

具体版本在初始化时选择兼容的稳定版本并锁定。RN 版本需考虑鸿蒙适配支持范围，不直接跟随主线最新版。本文件不声称已完成安装或构建验证。

## 4. 跨端路线与端能力扩展

移动端暂缓后，本节路线调整为：首版交付电脑三栏 + 手机浏览器知乎式单栏（纯 React DOM）；原生移动端（Android、iOS、鸿蒙）后续启动时按 React Native 重建，复用后端接口与契约，不与首版 Web 共享 UI 组件。Web 壳已按第 9 节完成设计系统级重构（2026-08-29），桌面三栏和手机单栏均已落地。

- Web 内容登录后访问，首版优先使用 Vite 构建，不额外引入 SSR 运行时。
- 社区 Web 使用纯 React DOM：电脑三栏；手机为知乎式单栏，底部固定「首页 / 工具 / 发布＋ / 消息 / 个人页」，顶栏保留品牌、主题切换和搜索图标，右栏不常驻。不再通过 React Native Web 运行。
- Android/iOS 后续使用 RN 宿主；鸿蒙使用独立适配。共享接口 SDK、契约类型与业务规则，但不承诺共享 UI 组件或原生依赖。
- 管理后台独立使用 React DOM 与 Ant Design，不将后台组件放入社区 Web。
- 暂不绑定未经鸿蒙验证的 Expo 专有模块或服务；这不代表 Expo 不能用于独立的 Android/iOS 项目。
- 首版保持模块化单体，不引入微服务、Kubernetes、独立 Elasticsearch 或独立向量数据库。

### 4.1 端能力规则

业务层调用统一的能力接口，各平台注入自己的实现。[React Native 支持平台专用模块与文件](https://reactnative.dev/docs/platform-specific-code)。

| 能力 | Web 首版 | 后续原生实现 |
| --- | --- | --- |
| 图片选择 | 浏览器文件选择；HEIC 上传后由服务端转 jpeg | Android/iOS 相册模块、鸿蒙对应实现 |
| 复制链接 | 浏览器剪贴板 | 各端剪贴板接口 |
| 打开网址 | 浏览器导航 | 系统浏览器或应用内打开 |
| 拍照、系统分享、推送、定位 | 首版不默认新增 | 按实际需求增加能力接口、权限请求与各端实现 |
| 会话存储 | 两套 HttpOnly Cookie（社区 / 后台） | 移动端安全存储与独立认证适配 |

- 权限拒绝、设备不支持或依赖缺失时，业务层收到明确的不可用状态，不伪造成功。
- 只有某个平台需要的功能可以独立实现，不强制给所有平台添加同名空实现。
- 浏览器 API、Android/iOS 原生模块、鸿蒙 ArkTS 模块分别封装；Go 后端仍负责业务权限与内容审核。
- 不预先引入尚未使用的原生 SDK；开发需要某项能力时再实现和测试对应适配器。

## 5. 模块与数据边界

建议后端模块：账号、资料、学生身份认证、权限、认证徽标、帖子、帖子版本、评论、标签、互动、审核、会话（社区/后台分离）、消息、通知、百宝箱、平台公告、热门话题配置、官方资料、知识检索、审计。

推荐仓库结构（规划，部分目录已创建）：

~~~text
apps/
  web/                 # 纯 React DOM 社区入口，首版实现（电脑三栏 + 手机单栏）
  admin/               # React DOM 管理后台，首版实现
  native/              # 暂缓：后续 Android/iOS RN 宿主
  harmony/             # 暂缓：后续鸿蒙宿主
packages/
  contracts/           # 协议、客户端类型与共享枚举
  api-client/          # TypeScript API 客户端
  features/            # 共享数据与交互逻辑
  ui/                  # 社区 Web 基础组件（React DOM）
  capabilities/        # 按需增加端能力接口与实现
server/
  cmd/api/             # Go API 入口
  cmd/worker/          # Go 审核与文档处理 Worker
  internal/            # 业务、权限、外部适配与数据访问
  migrations/          # 版本化数据库迁移（goose）
infra/                 # 部署、备份与运维配置
~~~

关键约束：

1. 公开资料与内部资料分开返回，不能仅在前端隐藏姓名、学号和班级。
2. 徽标和权限分开；私信正文读取不能继承普通身份资料读取权限。
3. 审核任务绑定内容版本；旧回调不能覆盖新编辑，也不能解除管理员下架。
4. API、搜索、图片、通知与分享链接统一校验内容状态和访问权限。
5. 私信首次招呼与双向解锁通过数据库事务和唯一约束维护，不只依赖按钮禁用。
6. 异步任务可能重复执行，审核结果、通知和消息送达必须幂等；对外发布与业务持久化需有一致性机制。
7. 短信、AI、对象存储凭证只在服务端。服务未配置时明确不可用，不能默认审核通过。
8. 百宝箱中的校园地图首版只展示有权使用的地图图片，不实现点位查询、室内定位、路线导航或直接读取学校系统。
9. 手机号全站唯一；管理员登录名全站唯一；公开昵称在未删除账号中全站唯一。
10. 社区会话与后台会话隔离，不能因社区已登录而获得后台权限。

## 6. 一个月开发顺序建议

这是一份排期目标，不是已验证工期承诺；开发者熟悉程度、实际投入时间、外部审批与测试发现会影响交付。

| 时间 | 工作重点 | 可验证交付 |
| --- | --- | --- |
| 第 1 周 | 工程、数据库、账号、资料、权限、后台框架 | 短信边界与邀请注册可测；非授权用户不能读取内部字段 |
| 第 2 周 | 图文发帖、标签、版本、审核、评论、点赞收藏 | 审核失败不公开；编辑期间旧版仍可读；后台下架有效 |
| 第 3 周 | 私信申请与文字聊天、互动通知、搜索、百宝箱入口 | 双向内置消息解锁规则、审核后送达、登录分享链接可测；搜索仅返回公开帖 |
| 第 4 周 | 官方资料、校园问答、百宝箱/热门话题/公告配置、联调、部署与修复 | 回答可追溯来源；真实短信/审核服务联调；完成回归与小范围试用 |

外部账号与服务申请应在第 1 周启动，而不是等第 4 周；不自动购买或开通付费云资源。

## 7. 验收重点

- 120 秒发送间隔、300 秒有效期、3 次错误上限均以服务端时间与原子状态为准；校验成功后验证码不可复用。
- 资料未完善或学生认证未通过的账号，不能发帖、评论、点赞、收藏、发送私信或使用 AI 问答；浏览、举报、资料修改和认证申请仍可用。
- 认证申请由获授权的管理员人工审核；资料完整或认证通过都不能绕过禁言、封禁等独立限制。
- 同一学号不能同时绑定两个已认证账号；后台绑定、解绑须校验权限，并发操作不能绕过唯一性约束。
- 解绑学号立即撤销学生认证并停止互动和 AI 问答权限，已有会话也必须受限；重新绑定不能绕过人工认证或解除独立处罚。
- 已认证用户不能通过前端或直接调用接口修改真实姓名、学号，必须走管理员处理的变更申请流程；头像、性别、昵称、班级可自行修改。
- 性别仅允许男/女；公开主页照实显示。
- 认证申请每次 1 张证明图（有信息的一面），审核结束满 30 天后自动清理对应证明材料，认证记录长期保留且认证状态不因材料清理失效。
- 注销后学号释放，允许重新注册认证；主库内部资料物理删除，认证记录匿名化。
- 手机号全站唯一；管理员后台登录名 + 密码，绑手机后才能进社区；解绑手机即撤销学生认证且不能登社区，后台不受影响。
- 社区与后台两套会话；社区已登录不能直接进入后台。
- 已绑手机可用短信重置密码；未绑手机的管理员由超管改密。
- 已认证学生的资格不因毕业自动失效；多名管理员按各自授权操作，不能越权管理或自行修改授权。
- 支持自定义职责角色及一名管理员兼任多个角色；管理员的管理操作进入专门的操作日志，包括超级管理员的角色配置与授权变更。
- 操作日志分 `security` / `operational` 两档；安全归档至少六个月且默认仅超管可查；普通操作日志展示 15 天、90 天物理删除；不能被 15 天展示窗口提前删掉安全归档。
- 普通账号、认证账号、仅有身份资料读取权限的账号都不能越权操作后台或读取私信。
- 自动审核未明确通过的内容不泄露到公开帖子、搜索、图片地址或通知正文。
- AI 失败时帖子/评论进入未公开人工队列；私信未送达，不伪造成功。
- 编辑稿通过前旧版本保持可见；并发编辑、管理员下架和延迟审核回调不导致错误发布。
- 并发点击只能生成允许数量的内置私信；接收方仅查看不会解锁，自由消息在双向确认前被服务端拒绝。
- 分享按钮只复制链接；未登录打开不能读取正文，登录后回到目标页。
- 删除主评论保留占位和其他人的回复；删除整帖则删除全部关联评论、回复，并阻止任何用户恢复。延迟审核结果不能重新发布已删除内容。
- 首次举报后临时隐藏，无法判定时继续隐藏并转人工；复核无违规时自动解除举报隐藏，但不能恢复作者已删除或独立被管理员下架的内容。
- 已复核无违规的同一内容版本再次收到举报时，不自动隐藏；管理员决定是否重新隐藏，且不能因此解除其他独立的访问限制。
- 封禁账号可登录并使用处罚通知与申诉入口；后端拒绝其访问其他社区功能。
- 禁言在后端禁止发帖、编辑帖子、评论、回复和发送私信，不影响符合各自准入条件的浏览、点赞、收藏、AI 问答、举报和申诉。
- 帖子图片上限为 9 张，评论和回复图片上限均为 1 张；仅 jpg/png/webp/heic（HEIC 转 jpeg），单张 ≤ 10MB；前后端分别校验，上传不能绕过审核和访问权限。
- 搜索只返回已公开帖子（正文或标签命中）；不搜评论、用户、私信或 AI 会话。
- 作者可在「我的帖子」看到待审/未通过；可在原帖看到自己的待审评论；他人、搜索、信息流不能看到未公开内容。
- 封号不自动隐藏已公开的历史帖子和评论。
- 标签软删后允许同名重建，旧帖不会自动挂到新标签。
- 问答优先返回核验数据与原始来源；无资料或资料冲突时不编造部门电话、日期或发布单位。
- AI 支持连续追问与按账号保存会话，换设备后可继续；每次完成的回答扣 1 次，失败或超时不扣，各设备共用每日 10 次额度。
- 用户可选择后台开放的模型，并在同一会话中切换后继续追问；服务端拒绝调用未向用户开放的模型，切换不覆盖原会话已有回答。
- 选中模型不可用时，按照后台配置的备用模型顺序自动切换；一次最终完成的回答仍按一次扣额，全部失败不扣，不能把每次内部尝试分别扣给用户。
- 普通用户不能访问他人的 AI 会话，管理员不能通过后台或接口读取用户完整 AI 对话；待补充问题入口不能扩展为整段会话查询。
- 用户删除整段 AI 会话后，联网设备同步移除，离线设备在重新联网同步时移除；历史读取和续聊接口不再提供该会话，不提供用户恢复入口。已发出的官方回答通知仍可查看摘要与官方答案；知识库答案保留。
- 未主动删除的 AI 对话不因保存时长被自动清理；删除会话同时撤回关联未处理问题，后续官方回答通知不得通过延迟任务重新发送给该用户。
- 发送给模型的上下文按目标模型上限从最近轮次截断，不把被截掉的旧消息摘要后塞回，也不在界面上假装模型读过。
- 校内知识库未命中时记录待补充问题，即使联网已给出答案也保留资料缺口；管理员发布补充答案后通知未撤回相应问题的原提问用户，不能将问题或通知泄露给无关账号。
- 假数据和模拟服务仅用于开发测试，必须清楚标识，不能作为正式上线完成的证据。

## 8. 尚未确认、但不必继续阻塞技术选型的事项

2026-08-28 两轮拷问已确认大量事项（见各节"已确认"标注），本节仅保留仍未确认的剩余项：

- 预算限额议题按用户要求暂缓，不把先前建议的预算告警或自动停用作为已确认规则；首批按 100 名已认证用户估算，初始月度目标为服务器 100 元、其他服务合计 100 元，实际费用与扩容仍待运行验证。
- AI 首批供应商与默认模型为后台运行时配置项，不在开发期锁死（上下文截断策略已确认）。
- 域名等一次性费用的预算归属仍待确认。
- 隐私政策与服务协议文本需在上线前审定（已确认由开发侧起草初稿、项目方上线前审定）。

上述未确认事项应以可修改的配置或明确待办管理，不应被包装成用户已确定的规则。

## 9. UI 设计决策（2026-08-28 确认，2026-08-29 修订）

首版交付电脑三栏 + 手机浏览器可用单栏；原生移动端原型已完成、开发暂缓。Web 壳已按本表完成设计系统级重构（2026-08-29，落地细节见 9.1）。设计交付流程为：先出可视化原型确认，确认后再写代码。

| 决策项 | 结论 |
| --- | --- |
| 视觉风格 | 清爽明亮，大留白、圆角卡片、轻阴影；2026-08-29 修订：外壳取小红书风格（品牌红、胶囊按钮、圆角卡片），中间信息流取知乎风格（单列流、赞同胶囊、幽灵操作按钮） |
| 主色 | 品牌红 #ff2442（2026-08-29 修订，小红书系，替代原蓝色系 #2563EB），用于按钮、链接、选中态、未读冒泡；配 hover/深色变体与 soft 底色梯度 |
| PC 布局 | 三栏：左导航 + 用户卡片 / 中信息流 / 右侧栏（百宝箱入口 + 平台公告 + 热门话题） |
| 手机 Web 布局 | 知乎式单栏信息流；底部固定五栏「首页 / 工具 / 发布＋ / 消息 / 个人页」，中间发布键凸起并进入独立发帖页；顶栏保留品牌、日夜切换和搜索图标；右栏不常驻，公告和热门话题仅在首页可折叠块 |
| 发帖 | 独立发帖页；2026-08-29 修订：电脑端仅保留左栏一个入口，顶栏发帖入口移除、顶栏右侧改为日夜间切换按钮；不内嵌信息流、不使用悬浮键 |
| 搜索 | 独立搜索页；2026-08-29 修订：电脑顶栏搜索框居中显示、内嵌红色「搜索」按钮，手机顶栏图标进入；只搜公开帖正文和标签 |
| 信息流 | 2026-08-29 修订：知乎式单列流（明确否决小红书瀑布流）——作者行 + 15px 正文（3 行截断）+ 自适应图片网格 + 红色话题文字链 +「赞同 n」软胶囊与幽灵操作按钮；置顶帖独立展示（品牌红描边），普通帖按发布时间倒序，不提供热度排序 |
| 卡片图片 | 自适应网格：1 图大图 / 2–4 图两列 / 5–9 图三列 |
| 百宝箱 | 对外名称；含校园地图、AI 问答、常用网址；顺序超管手配 |
| 热门话题 | 超管手动选标签和顺序，不自动算热度 |
| 平台公告 | 独立公告，非置顶帖、非校方通知 |
| 我的 | 资料卡、我的帖子、收藏、编辑资料、认证/变更、注销、退出；有权限则「进入管理后台」 |
| 公开主页 | 头像、昵称、性别、认证徽标、已公开帖 |
| 移动端导航（原生原型） | 五入口底部导航，中间凸起圆形发布键；首版 Web 不采用 |
| 移动端首页（原生原型） | 顶部工具横排 + 下方信息流；首版 Web 不采用 |
| 交付流程 | 先出可视化原型确认，再进入开发 |

### 9.1 前端设计系统落地（2026-08-29 确认）

Web 壳（`apps/web`，React 18 + Vite + TypeScript + CSS Modules）已完成全站设计系统级改造，`tsc -b && vite build` 构建通过并经浏览器走查验证。既有业务逻辑与接口未改动（仅按用户明确要求新增未读同步与主题切换）。

- 设计令牌（`src/styles/global.css`）：浅色主色 `--brand:#ff2442`，含 hover/dark 变体与 `--brand-soft`/`--brand-softer`/`--brand-border` 底色梯度；表面色 `--surface`/`--surface-subtle`（页面底）/`--surface-blue`；文字 `--text`/`--text-muted`/`--text-soft`；边框 `--border`/`--border-light`；状态色 `--danger`/`--success`/`--success-soft`；圆角梯度 `--r-sm:8`/`--r-md:12`/`--r-lg:16`/`--r-pill:999`。全站 module.css 硬编码色已清扫换用令牌（彩色底上的白字、深色遮罩、地图插画色等有意保留）。
- 日夜间双主题：`[data-theme="dark"]` 覆盖全套令牌（深色下品牌红提亮为 #ff3b57，表面/文字/边框整体反转），`color-scheme` 同步。顶栏右侧为日/夜切换按钮（替代原发帖入口）；选择持久化到 localStorage（`xsnbb-theme`），首次访问跟随系统 prefers-color-scheme。
- 顶栏：三列网格布局，搜索框居中（内嵌红色「搜索」键）；右侧为日夜间切换、消息铃铛（带未读数字冒泡）、用户头像。左侧栏保留唯一「发布帖子」入口。
- PostCard 双变体（`variant: 'feed' | 'detail'`，默认 feed）：feed 为知乎信息流卡（首页/搜索/个人主页/公开主页列表共用），detail 为知乎阅读卡（详情页：16px/1.75 正文、全宽图、完整操作条）；帖子图片带加载失败兜底占位。
- 消息未读机制：铃铛与导航「消息」均显示红色数字冒泡；未读数 = 通知未读（服务端通知接口 unread，30 秒轮询）+ 私信未读（本地计算）。通知点开即调已读接口并同步全站冒泡；私信进入会话即标记已读。
- 私信未读为本地计算（zustand persist store `xsnbb-dm-read`，按会话记录最后已读时间，最后一条对方消息晚于已读时间即未读）；换设备不同步，属已知局限，服务端私信未读口径后续补齐。
- 桌面端（约 1040px 起）布局已定型验证；手机 Web 已完成知乎式单栏布局、底部五栏、独立搜索入口和首页折叠信息块适配。中间凸起发布键继续复用独立发帖页；底栏和内容留白适配刘海屏安全区。
- 已知遗留：演示数据图片 `/uploads/demo/*.jpg` 在服务端不存在（后端遗留），前端已做兜底，不影响功能。
